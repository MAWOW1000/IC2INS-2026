---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { conference, registrationFees, registrationNotes } from '../../../lib/ic2ins-data.js';

const title = `Registration Amount | ${conference.shortName}`;
const description = `${conference.shortName} registration amount for authors, delegates, industry participants, and attendees.`;
const refundNote = 'Registration fee once submitted cannot be refunded.';
const registrationListNotes = registrationNotes.filter((note) => note !== refundNote);
---

<BaseLayout
  title={title}
  description={description}
  canonical={new URL('/author-info/registration-amount', Astro.site).toString()}
>
  <div id="content" class="col-full business">
    <div id="main-sidebar-container">
      <section id="main" style="width: 98%;">
        <article class="post-2 page type-page status-publish hentry">
          <section class="entry">
            <div class="woo-sc-hr" style="border-bottom: 6px solid #efefef; width: 100%;">
              <h2>Registration Amount</h2>
            </div>
            <table border="1" width="100%">
              <thead>
                <tr>
                  <th align="left">Category</th>
                  <th align="left">For Indian Authors and Delegates (INR)</th>
                  <th align="left">For Foreign Authors and Delegates (USD)</th>
                </tr>
              </thead>
              <tbody>
                {
                  registrationFees.map((row) => (
                    <tr>
                      <td>{row.category}</td>
                      <td>{row.inr}</td>
                      <td>{row.usd}</td>
                    </tr>
                  ))
                }
              </tbody>
            </table>
            <ul>
              {registrationListNotes.map((note) => <li>{note}</li>)}
            </ul>
            <p class="author-note">
              <strong>NOTE:</strong> Registration fee once submitted, it cannot be refunded.
            </p>
          </section>
          <div class="fix"></div>
        </article>
      </section>
    </div>
  </div>
</BaseLayout>
